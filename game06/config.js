// Настройки раннера (упрощённая, проходимая версия)
window.NINJA_RUNNER_CONFIG = {
    TARGET_KILLS: 40,          // меньше нужно поверженных
    SPEED_PX_S: 190,          // медленнее фон
    GRAVITY: 1500,            // физика чуть плотнее
    JUMP_VELOCITY: 800,       // выше прыжок
    ATTACK_RANGE_PX: 110,     // удар «дальше»
    MAX_DISTANCE_M: 300,      // короче дистанция

    // Спавним ОДИН объект за раз, с паузой:
    SPAWN_GAP_MS: [800, 1200], // пауза между объектами
    ENEMY_PROBABILITY: 0.6,    // шанс, что следующим будет враг

    // Ограничения по количеству одновременно на сцене:
    MAX_ACTIVE_ENEMIES: 2,
    MAX_ACTIVE_OBSTACLES: 1
};
